upstream iway_backend_staging {
  server backend:3001;
  keepalive 32;
}

server {
  listen 80;
  server_name staging-api.example.com;
  return 301 https://$host$request_uri;
}

server {
  listen 443 ssl http2;
  server_name staging-api.example.com;

  ssl_certificate /etc/letsencrypt/live/staging-api.example.com/fullchain.pem;
  ssl_certificate_key /etc/letsencrypt/live/staging-api.example.com/privkey.pem;
  ssl_protocols TLSv1.2 TLSv1.3;
  ssl_prefer_server_ciphers on;

  client_max_body_size 20m;

  location /api/health/live {
    proxy_pass http://iway_backend_staging/api/health/live;
    proxy_set_header Host $host;
  }

  location /api/ {
    proxy_pass http://iway_backend_staging/api/;
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto https;
    proxy_read_timeout 120s;
  }

  location /socket.io/ {
    proxy_pass http://iway_backend_staging/socket.io/;
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
    proxy_set_header Host $host;
    proxy_read_timeout 120s;
  }
}
