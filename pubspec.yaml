name: iway_app

description: App IWAY - Envíos Guatemala ↔ USA

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  cupertino_icons: ^1.0.2
  firebase_core: ^3.13.0
  firebase_messaging: ^15.2.5
  flutter_local_notifications: ^17.2.1+2
  image_picker: ^1.0.7
  google_maps_flutter: ^2.6.0
  geolocator: ^11.0.0
  http: ^1.2.1
  path_provider: ^2.1.4
  pdf: ^3.11.1
  printing: ^5.13.4
  shared_preferences: ^2.2.3
  socket_io_client: ^3.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true